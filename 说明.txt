1.今天晚上的主要任务是将Fastest Gaussian Blur的算法1，因为后面的算法都是一样的调用方式，会第一种算法的调用方式后面的就简单了。
实现这一目标：（1）将图片读入并转换为矩阵的形式，第一步本来想用库文件的方式来解决，比如labjpeg，但是感觉不如用OpenCV的Mat数据类型简单。所以还是用OpenCV的方式吧。
              （2）调用该算法
              （3）通道合并得到处理结果即模糊图像。
2.1.10从移动硬盘里转出来的。